import AppConfig from './AppConfig';
import GatewayAnswer from './GatewayAnswer';
import GatewayRequest from './GatewayRequest';
export default class Service {
    private config;
    private app;
    private serviceName;
    private endpoints;
    constructor(serviceName: string, config?: AppConfig);
    start(): void;
    endpoint(path: string, handler: (request: GatewayRequest, answer: GatewayAnswer) => Promise<GatewayAnswer>): void;
    private sanitize;
    private initExpress;
    private isAnswerValid;
    private initDefaultRouting;
}
